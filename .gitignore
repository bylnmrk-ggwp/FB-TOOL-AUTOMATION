__pycache__/
*.pyc
.autoshare/
autoshare_config.enc
.env
.venv/
venv/
*.log
dump_*.json
nul
.claude/settings.local.json

# Account spreadsheets - plaintext credentials, never commit
*.xlsx
*.xls
*.csv

# Google service-account key — never commit
.secrets/
*service-account*.json
# service-account key (exact name)
mcarsmainwebsite-01d2cff94ed8.json

# parallel-login scratch + profile backups (session data)
.parallel-work/
.parallel-backup/
node_modules/

# web app build output and the frp tunnel (binary + the toml with the token)
web/node_modules/
web/dist/
tools/frp/frpc.toml
tools/frp/*.exe
tools/frp/*.zip

# Exported Facebook sessions - plaintext, never commit
transfer/

# Windows folder metadata, MCP scratch, dead parallel-login output
desktop.ini
.playwright-mcp/
.parallel-backup/
checkpoint_probe.png
