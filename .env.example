# Copy to .env and fill in. .env is gitignored - never commit real keys.

# Groq API key, used for AI gender detection and the friendship-network analysis
# summary. Optional: without it both features fall back to local logic.
# Get one at https://console.groq.com/keys
GROQ_API_KEY=
